#include <stdio.h>
int main()
{
    int num;
    printf("Enter number of rows: ");
    scanf("%d", &num);

    for(int i = 1; i <= num; i++)
    {
        // Print spaces
        for(int j = 1; j <= num - i; j++)
        {
            printf(" ");
        }
        // Print stars
        for(int k = 1; k <= i; k++)
        {
            printf("* ");
        }
        printf("\n");
    }

    return 0;
}

output:-

Enter number of rows: 4
   * 
  * *
 * * *
* * * *
