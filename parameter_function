/* #include<stdio.h>
        void add(int a,int b)
        {
            printf("%d",a+b);
        }
        int main()
        {
            add(90,10);
        }
     */

     #include<stdio.h>

     void min(int x,int y)
     {
        if(x > y)
        {
            printf("X is a larger");
        }
        else
        {
            printf("Y is a larger ");
        }
    }
        int main()
        {
            min(23,45);
        }


output:-Y is a larger 
