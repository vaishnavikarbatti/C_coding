/* #include<stdio.h>
    
    int circle()
    {
        int radius;
        int area;

        printf("Enter an radius value :");
        scanf("%d",&radius);

        area  = 3.14 * radius * radius;
        printf("Area of circle is : %d",area);
    }
    int main()
    {
        circle();
    }
     */

     #include<stdio.h>

     void circle(int radius)
     {
        int area;

        area=3.14*radius*radius;
        printf("Area of circle is :%d",area);
     }
     int main()
     {
        circle(4);
     }  

output:-Area of circle is :50
