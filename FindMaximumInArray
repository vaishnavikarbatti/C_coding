#include <stdio.h>

int main() {
    int arr[] = {1, 2, 3, 4, 5, 67};
    int n = sizeof(arr) / sizeof(arr[0]); // Correct way to calculate size

    int max = arr[0];
    for (int i = 1; i < n; i++) {
        if (max < arr[i]) {
            max = arr[i];
        }
    }

    printf("Maximum number from array is: %d", max);

    return 0;
}

output:-
Maximum number from array is: 67
